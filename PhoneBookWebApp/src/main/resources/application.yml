server:
  port: 2222
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/projectdb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        format_sql: true
    show-sql: true
